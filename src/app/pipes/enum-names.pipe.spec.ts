import { EnumNamesPipe } from './enum-names.pipe';

describe('EnumNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
